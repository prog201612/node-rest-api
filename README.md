# Init
```bash
mkdir node-rest-api
cd node-rest-api
npm init
```

# Libraries
```bash
npm install express -E # framework
npm install zod -E # validate
npm install cors -E # CORS
```

# package.json
```json
"scripts": {
    "start": "node app.js"
}
```

